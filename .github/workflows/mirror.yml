name: Mirror to GitLab
on:
  push:
    branches: [main]
    tags: ['**']
  workflow_dispatch:
permissions:
  contents: read
concurrency: mirror
jobs:
  mirror:
    runs-on: ubuntu-latest
    # The deploy key is the only secret this repo uses, and it lives in a
    # dedicated environment so it can be scoped and reviewed on its own
    # rather than being readable by every job in the repo. zizmor's
    # secrets-outside-env audit flags the alternative, and this repo runs
    # that audit against itself.
    environment: gitlab-mirror
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Push to GitLab
        env:
          KEY: ${{ secrets.GITLAB_DEPLOY_KEY }}
        if: env.KEY != ''
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$KEY" > ~/.ssh/mirror_key
          chmod 600 ~/.ssh/mirror_key
          ssh-keyscan gitlab.com >> ~/.ssh/known_hosts 2>/dev/null
          git remote add gitlab git@gitlab.com:munzzyy/ci-safety-gate.git
          GIT_SSH_COMMAND='ssh -i ~/.ssh/mirror_key' git push gitlab HEAD:refs/heads/main --tags --force
